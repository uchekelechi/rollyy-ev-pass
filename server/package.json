{
  "name": "rollyy-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Rollyy API — charging, parking, maintenance and car wash discovery for the Rollyy mobile app.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "start": "node --env-file=.env src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "express-rate-limit": "^7.4.1"
  }
}
